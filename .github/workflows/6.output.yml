name: 6.Output_information Expressions
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: output GitHub context
        run: echo "${{ toJSON(github) }}" #github expressions toJSON
